let divisions = 100;
let barArray = Array(divisions);
let DerivArray = Array(divisions);
let secondDeriv = Array(divisions);

function initBarArray(){

    for (let x = 0 ; x < divisions ; x++){
        barArray[x] = Math.sin(Math.PI*x/50);
    }

}

function updateBarArray(){

    for (let x = 0 ; x < divisions ; x++){
        secondDeriv[x] = 
    }

    barArray[0] = 1
}

function drawBarArray(){

}